<div align="center">

**🌐 Select Language / 选择语言**

[English](README.md) | [中文](README_CN.md) | [Español](README_ES.md) | [Русский](README_RU.md) | [Bahasa Indonesia](README_ID.md) | [Türkçe](README_TR.md) | **Tiếng Việt** | [ภาษาไทย](README_TH.md) | [हिन्दी](README_HI.md) | [اردو](README_UR.md) | [Filipino](README_PH.md)

</div>

---

# binance-p2p-bot：binance&bybit&okx P2P auto_payment_bot: Bot Thanh Toán Tự Động Theo Đơn · Xác Minh Hóa Đơn · Giải Phóng Crypto

> Công cụ tự động hóa chuyên nghiệp dành cho thương nhân P2P Binance, Bybit & OKX — **thanh toán tự động theo đơn**, **xác minh hóa đơn thông minh**, **giải phóng crypto trong mili giây**, xử lý song song đa thiết bị.

# [Bot Thanh Toán & Giải Phóng Tự Động P2P (Binance & Bybit & OKX) Trang Chủ](https://apip2p.top)

## 🎬 Video Demo


https://github.com/user-attachments/assets/262a41b2-cdcc-4abd-8c8e-4bdb1b566a03


[![Xem Demo](https://img.shields.io/badge/YouTube-Xem_Demo-red?style=for-the-badge&logo=youtube&logoColor=white)](https://www.youtube.com/watch?v=INewAPHXa5c)

> 📺 Xem video demo đầy đủ `binance&bybit&okx P2P auto_payment_bot` hoạt động — từ phát hiện đơn hàng đến thanh toán tự động, xác minh hóa đơn và giải phóng crypto.

Giải pháp thanh toán đơn hàng tự động tối ưu cho thương nhân P2P (nhà quảng cáo). Loại bỏ chu trình chuyển khoản thủ công, xác minh thủ công và giải phóng crypto thủ công. Với `binance&bybit&okx P2P auto_payment_bot`, đạt được **tự động hóa hoàn toàn 24/7** — thanh toán theo đơn, xác minh tiền gửi và giải phóng crypto tức thì.

---

## 📝 Tổng Quan & Kiến Trúc


<img width="1315" height="846" alt="TR3" src="https://github.com/user-attachments/assets/669affee-5698-4b92-bab5-67b847c8c943" />
<img width="1360" height="881" alt="2" src="https://github.com/user-attachments/assets/b2c18af8-6b0f-4062-a947-bb10e99aeed7" />


`binance&bybit&okx P2P auto_payment_bot` là một **ứng dụng máy tính bàn tự động hóa cục bộ** chuyên nghiệp được xây dựng cho Thương Nhân P2P (Nhà Quảng Cáo) trên **Binance**, **Bybit** và **OKX**.

Trong giao dịch P2P, thương nhân phải đối mặt với các thao tác lặp lại:

- ❌ **Thanh Toán Thủ Công**: Mở ứng dụng thanh toán, nhập số tiền, xác nhận chuyển khoản
- ❌ **Xác Minh Thủ Công**: Mở ứng dụng kiểm tra người mua đã thanh toán chưa
- ❌ **Giải Phóng Thủ Công**: Quay lại sàn giao dịch bấm nút giải phóng
- ❌ **Khoảng Trống Ban Đêm**: Đơn hàng tích tụ ngoài giờ làm việc

`binance&bybit&okx P2P auto_payment_bot` giải quyết tất cả thông qua **tự động hóa thiết bị thông minh + tích hợp API sàn giao dịch**:

- ✅ **🔥 Thanh Toán Tự Động Theo Đơn (Tính Năng Cốt Lõi)**: Sàn nhận đơn mua → tự động hoàn tất chuyển khoản trong ứng dụng thanh toán → không cần can thiệp
- ✅ **Xác Minh Hóa Đơn Thông Minh**: Người mua đánh dấu đã thanh toán → tự động mở ứng dụng xác minh số tiền và thông tin người gửi
- ✅ **Giải Phóng Crypto Mili Giây**: Sau xác minh thành công, gọi API sàn ngay lập tức
- ✅ **Xử Lý Song Song Đa Thiết Bị**: Kết nối nhiều điện thoại đồng thời
- ✅ **Quản Lý Thống Nhất Đa Sàn**: Binance, Bybit, OKX — một giao diện duy nhất
- ✅ **Hoạt Động Hoàn Toàn Cục Bộ**: Không phụ thuộc máy chủ ngoài

**Định vị**: `binance&bybit&okx P2P auto_payment_bot` xử lý **thanh toán theo đơn + xác minh hóa đơn + giải phóng tự động**. Kết hợp với [`binance_p2p_bot`](https://github.com/ApiP2P-top/binance-p2p-bot) để đạt **tự động hóa thông minh end-to-end**.

---

## 🚀 Tính Năng Cốt Lõi

### 1. 🔥 Thanh Toán Tự Động Theo Đơn

> **Đây là khả năng quan trọng nhất và khác biệt nhất của `binance&bybit&okx P2P auto_payment_bot`.**

- **Phát Hiện Đơn Hàng Real-time**: Chu kỳ polling 5 giây
- **Điều Phối Thiết Bị Thông Minh**: Tự động khớp thiết bị rảnh theo phương thức thanh toán
- **Thực Thi Thanh Toán Tự Động**: Hoàn tất toàn bộ quy trình chuyển khoản
- **Bộ Thanh Toán Thích Ứng**: Bộ giao thức thông minh tự động thích ứng với giao diện của từng ứng dụng thanh toán
- **Hỗ Trợ Đa Phương Thức**: Một thiết bị, nhiều phương thức thanh toán
- **Lưu Trữ Ảnh Chụp**: Tự động chụp bằng chứng sau mỗi giao dịch

#### 💡 Khái Niệm Triển Khai (Bộ Lập Lịch Chuyển Khoản)

```python
# [Pseudocode] Order detection → device matching → auto transfer scheduling pipeline
import asyncio
from dataclasses import dataclass
from typing import Optional, List

@dataclass
class TransferTask:
    order_id:       str
    amount:         float
    currency:       str
    payee_name:     str
    payee_account:  str
    payment_method: str
    status:         str

class TransferScheduler:
    POLL_INTERVAL = 5

    def __init__(self, exchange_clients: dict, device_pool: 'DevicePool'):
        self.exchanges   = exchange_clients
        self.device_pool = device_pool

    async def run(self):
        while True:
            for platform, client in self.exchanges.items():
                orders = await client.get_pending_orders()
                for order in orders:
                    if self._is_new_order(order):
                        await self._dispatch(order)
            await asyncio.sleep(self.POLL_INTERVAL)

    async def _dispatch(self, order) -> None:
        device = self.device_pool.find_idle_device(order.payment_method)
        if not device:
            return
        task = TransferTask(
            order_id=order.id, amount=order.fiat_amount,
            payee_name=order.counterparty, payee_account=order.account,
            payment_method=order.payment_method, status="pending",
        )
        await device.execute_transfer(task)
```

```javascript
// [Pseudocode] Node.js style — Order polling and device dispatch
class TransferScheduler {
    constructor(exchangeClients, devicePool) {
        this.exchanges  = exchangeClients;
        this.devicePool = devicePool;
        this.pollInterval = 5000;
    }

    async start() {
        while (true) {
            for (const [platform, client] of Object.entries(this.exchanges)) {
                const orders = await client.getPendingOrders();
                for (const order of orders) {
                    if (this.isNewOrder(order)) {
                        await this.dispatch(order);
                    }
                }
            }
            await this.sleep(this.pollInterval);
        }
    }

    async dispatch(order) {
        const device = this.devicePool.findIdleDevice(order.paymentMethod);
        if (!device) return;
        await device.executeTransfer({
            orderId: order.id,
            amount: order.fiatAmount,
            payeeName: order.counterparty,
            paymentMethod: order.paymentMethod,
        });
    }
}
```

---

### 2. Xác Minh Hóa Đơn Thông Minh

- **Điều Hướng Hóa Đơn Tự Động**: Mở ứng dụng đến trang lịch sử giao dịch
- **Khớp Số Tiền Chính Xác**: Với dung sai có thể cấu hình
- **Xác Minh Người Gửi**: Đối chiếu chéo tên người gửi
- **Trích Xuất ID Giao Dịch**: Đọc Reference Number / mã giao dịch
- **Bảo Vệ Anti-Replay**: Mỗi ID được ghi nhận — một khoản tiền không thể kích hoạt hai lần giải phóng

#### 💡 Khái Niệm Triển Khai (Bộ Xác Minh Tiền Gửi)

```go
// [Pseudocode] Go style — Deposit verification with replay-attack protection
package verifier

type BillVerifyResult struct {
    Matched bool; Amount float64; SenderName string; UniqueID string
}

type BillVerifier struct {
    archive *BillArchive; tolerance float64
}

func (v *BillVerifier) Verify(order P2POrder) (*BillVerifyResult, error) {
    bills, err := v.navigateToBillList(order.PaymentApp)
    if err != nil { return nil, err }
    for _, bill := range bills {
        if math.Abs(bill.Amount - order.FiatAmount) <= v.tolerance {
            detail := v.openBillDetail(bill)
            if v.archive.Exists(detail.UniqueID) { continue }
            return &BillVerifyResult{
                Matched: true, Amount: detail.Amount,
                SenderName: detail.SenderName, UniqueID: detail.UniqueID,
            }, nil
        }
    }
    return &BillVerifyResult{Matched: false}, nil
}
```

```typescript
// [Pseudocode] TypeScript style — Bill verification service
interface BillDetail {
    amount: number; senderName: string; uniqueId: string; timestamp: Date;
}

class BillVerifyService {
    private archive: BillArchive;
    private tolerance = 0.02;

    async verify(order: P2POrder): Promise<VerifyResult> {
        const bills = await this.navigateToBillList(order.paymentApp);
        const matched = bills.find(bill =>
            Math.abs(bill.amount - order.fiatAmount) <= this.tolerance
            && !this.archive.exists(bill.uniqueId)
        );
        if (!matched) return { success: false };
        this.archive.record(matched.uniqueId, order.orderId);
        return { success: true, amount: matched.amount,
            senderName: matched.senderName, uniqueId: matched.uniqueId };
    }
}
```

---

### 3. Giải Phóng Crypto Tự Động

- **Giải Phóng Tức Thì**: Gọi API sàn ngay sau xác minh
- **Xác Minh Kết Quả**: Kiểm tra trạng thái đơn hàng
- **Thử Lại Theo Cấp Số Nhân**: Tối đa 3 lần (2/4/8 giây)
- **Bộ Chuyển Đổi Ba Nền Tảng**: Binance / Bybit / OKX

#### 💡 Khái Niệm Triển Khai (Bộ Thực Thi Giải Phóng Đa Nền Tảng)

```python
# [Pseudocode] Cross-platform release executor
import asyncio, logging
logger = logging.getLogger("release_executor")

class PlatformAdapterFactory:
    @staticmethod
    def create(platform: str, credentials: dict):
        if platform == "binance": return BinanceP2PAdapter(credentials)
        elif platform == "bybit": return BybitP2PAdapter(credentials)
        elif platform == "okx": return OkxP2PAdapter(credentials)
        raise ValueError(f"Unsupported platform: {platform}")

class ReleaseExecutor:
    MAX_RETRIES = 3
    RETRY_BASE_S = 2

    async def release(self, platform: str, order_id: str, verify_result: dict) -> bool:
        adapter = PlatformAdapterFactory.create(platform, self.credentials)
        for attempt in range(self.MAX_RETRIES):
            try:
                resp = await adapter.release_order(order_id)
                if resp.success:
                    confirmed = await self._poll_completion(adapter, order_id)
                    if confirmed:
                        logger.info(f"[RELEASED] {platform} order {order_id}")
                        return True
            except Exception as e:
                wait = self.RETRY_BASE_S ** (attempt + 1)
                await asyncio.sleep(wait)
        return False
```

```rust
// [Pseudocode] Rust style — Release execution with retry
use std::time::Duration;

struct ReleaseExecutor { max_retries: u32, retry_base: Duration }

impl ReleaseExecutor {
    async fn release(&self, platform: &str, order_id: &str) -> Result<bool, Error> {
        let adapter = create_platform_adapter(platform)?;
        for attempt in 0..self.max_retries {
            match adapter.release_order(order_id).await {
                Ok(resp) if resp.success => {
                    if self.poll_completion(&adapter, order_id).await? {
                        return Ok(true);
                    }
                }
                Err(e) => {
                    let wait = self.retry_base * 2u32.pow(attempt);
                    tokio::time::sleep(wait).await;
                }
                _ => {}
            }
        }
        Ok(false)
    }
}
```

---

### 4. Xử Lý Song Song Đa Thiết Bị

- **Kết Nối Đa Thiết Bị**: USB / WiFi cho nhiều điện thoại Android
- **Phản Chiếu Màn Hình**: 30-60fps / 30-70ms độ trễ
- **Phân Công Thông Minh**: Tự động khớp thiết bị theo ứng dụng
- **Hàng Đợi Độc Lập**: Mỗi thiết bị có hàng FIFO riêng
- **Giám Sát Trạng Thái**: Kết nối, tác vụ hiện tại, số dư
- **Cách Ly Quy Trình**: Quy trình vận hành riêng biệt cho các thiết bị × ứng dụng × phương thức thanh toán khác nhau

#### 💡 Khái Niệm Triển Khai (Bộ Lập Lịch Song Song)

```python
# [Pseudocode] Multi-device task scheduler
from dataclasses import dataclass, field
from typing import Dict, List
from collections import deque

@dataclass
class Device:
    serial: str
    alias: str
    status: str
    supported_apps: List[str]
    task_queue: deque = field(default_factory=deque)

class DeviceScheduler:
    def __init__(self):
        self.devices: Dict[str, Device] = {}

    def find_idle_device(self, payment_method: str) -> Device | None:
        for device in self.devices.values():
            if device.status == "idle" and payment_method in device.supported_apps:
                return device
        return None

    def enqueue_task(self, task: 'TransferTask') -> bool:
        device = self.find_idle_device(task.payment_method)
        if device:
            device.task_queue.append(task)
            return True
        return False
```

```javascript
// [Pseudocode] JavaScript style — Device manager
class DeviceScheduler {
    constructor() { this.devices = new Map(); }

    findIdleDevice(paymentMethod) {
        for (const device of this.devices.values()) {
            if (device.status === 'idle' && device.supportedApps.includes(paymentMethod))
                return device;
        }
        return null;
    }

    enqueueTask(task) {
        const device = this.findIdleDevice(task.paymentMethod);
        if (device) { device.taskQueue.push(task); device.status = 'busy'; return true; }
        return false;
    }
}
```

---

### 5. Bộ Thích Ứng Thanh Toán Thông Minh

- **Hỗ Trợ Thanh Toán Không Cần Cấu Hình**: Bộ máy tự động nhận diện và thích ứng với giao diện của bất kỳ ứng dụng thanh toán nào
- **Không Cần Lập Trình**: Chỉ cần kết nối thiết bị và cấu hình phương thức thanh toán
- **Tối Ưu Hóa Đa Chiều**: Đường dẫn thực thi được tối ưu theo Thiết bị × Ứng dụng × Phương thức thanh toán
- **Tự Động Nhập Thông Tin Xác Thực**: Mật khẩu và mã PIN được nhập từ cấu hình mã hóa cục bộ khi chạy — không bao giờ lưu trong bộ nhớ đệm
- **Thực Thi Hai Chế Độ**: Thực thi thanh toán (chuyển khoản đi) và xác minh (kiểm tra tiền gửi đến) sử dụng pipeline xử lý độc lập

---

### 6. Kiểm Soát Rủi Ro & Chống Gian Lận

- **Loại Trùng ID Giao Dịch**: Lưu giữ 180 ngày
- **Khớp Số Tiền Chính Xác**: Từ chối nếu vượt dung sai
- **Bảo Vệ Thời Gian Chờ**: Không giải phóng nếu không tìm thấy tiền gửi
- **Giám Sát Số Dư**: Tự động tạm dừng khi số dư không đủ
- **Lưu Trữ Hoàn Toàn Cục Bộ**: Tất cả bản ghi giao dịch được lưu cục bộ (180 ngày lưu giữ, tự động dọn dẹp)

---

### 7. Lưu Trữ Trạng Thái & Khôi Phục Phiên

- **Tự Động Lưu**: Tất cả cấu hình được lưu real-time
- **Khôi Phục Sau Crash**: Tự động tải cấu hình cuối cùng
- **Khôi Phục Đơn Hàng Đang Chờ**: Quét đơn hàng chưa hoàn tất khi khởi động

---

## �️ Kiến Trúc Hệ Thống

```
┌─────────────────────────────────────────────────────────────────┐
│                     Cửa Sổ Chính GUI Desktop                       │
│  ┌───────────┐ ┌────────────┐ ┌────────────┐ ┌───────────┐  │
│  │ Quản Lý  │ │ Quản Lý     │ │  Phản Chiếu│ │ Log &     │  │
│  │ Sàn       │ │ Thiết Bị   │ │  (Màn Hình)│ │ Kiểm Toán │  │
│  └─────┬─────┘ └─────┬──────┘ └─────┬──────┘ └───────────┘  │
│        │             │             │                             │
│  ┌────▼───────────▼───────────▼──────────────────┐    │
│  │                Signal / Slot Event Bus                   │    │
│  └────┬───────────┬───────────┬──────────────────┘    │
│       │              │              │                              │
│  ┌────▼────┐ ┌─────▼─────┐ ┌────▼─────┐ ┌────────────┐   │
│  │ Đơn Hàng │ │ Bộ Thực   │ │Xác Minh   │ │ Lưu Trữ    │   │
│  │  Poller   │ │ Thi CK    │ │Hóa Đơn   │ │ Trạng Thái │   │
│  │(5s chu kỳ)│ │(thanh toán│ │(kiểm tra  │ │ (JSON/DB)   │   │
│  │          │ │ theo đơn) │ │ tiền gửi)│ │             │   │
│  └──────────┘ └───────────┘ └──────────┘ └────────────┘   │
│       │              │              │                              │
│  ┌────▼───────────▼───────────▼──────────────────┐    │
│  │        Anti-Replay & Kiểm Soát Rủi Ro                   │    │
│  │  ┌───────────┐ ┌───────────┐ ┌────────────────┐   │    │
│  │  │Dung Sai   │ │Loại Trùng │ │Giám Sát Số Dư  │   │    │
│  │  │Số Tiền    │ │ID Giao Dịch│ │& Hết Thời Gian │   │    │
│  │  └───────────┘ └───────────┘ └────────────────┘   │    │
│  └──────────────────────────────────────────────────┘    │
└───────────────────────────┬────────────────────────────────────┘
                            │
              ┌───────────▼──────────────┐
              │  Exchange API Adapter Layer   │
              │ ┌───────┐┌──────┐┌──────┐ │
              │ │Binance││Bybit ││ OKX  │ │
              │ └───────┘└──────┘└──────┘ │
              └──────────────────────────┘
```

---

## ⚙️ Thông Số Kỹ Thuật

| Thành Phần | Tham Số | Mô Tả |
|-----------|---------|--------|
| Chu Kỳ Polling | 5.000 ms | Polling độc lập cho mỗi sàn |
| Dung Sai Số Tiền | 0.01–0.10 | Có thể cấu hình |
| Thử Lại Giải Phóng | Backoff lũy thừa 2/4/8s | Tối đa 3 lần |
| Lưu Trữ Loại Trùng | 180 ngày | Tự động dọn dẹp |
| FPS Phản Chiếu | 30–60 fps | Độ trễ thấp (30-70ms) |
| Đa Thiết Bị | Không giới hạn | USB + WiFi |
| Hồ Sơ Thanh Toán | Lưu trữ cục bộ mã hóa | Tối ưu theo Thiết bị × App × Phương thức |
| Giao Diện | Tối / Sáng | Hai chế độ |
| Ngôn Ngữ | 中文 / English | Song ngữ |

### Xác Thực API & Ký Yêu Cầu

```python
# [Pseudocode] HMAC-SHA256 Request Signing
import hashlib, hmac, base64

class MultiPlatformAuth:
    def sign_binance(self, params: dict, secret: str) -> str:
        query = "&".join(f"{k}={v}" for k, v in sorted(params.items()))
        return hmac.new(secret.encode(), query.encode(), hashlib.sha256).hexdigest()

    def sign_bybit(self, timestamp: str, api_key: str, payload: str, secret: str) -> str:
        sign_str = f"{timestamp}{api_key}5000{payload}"
        return hmac.new(secret.encode(), sign_str.encode(), hashlib.sha256).hexdigest()

    def sign_okx(self, timestamp: str, method: str, path: str, body: str, secret: str) -> str:
        sign_str = f"{timestamp}{method}{path}{body}"
        return base64.b64encode(hmac.new(secret.encode(), sign_str.encode(), hashlib.sha256).digest()).decode()
```

```go
// [Pseudocode] Go style — Multi-platform API signing
package auth
import ("crypto/hmac"; "crypto/sha256"; "encoding/base64"; "encoding/hex"; "fmt"; "sort"; "strings")

func SignBinance(params map[string]string, secret string) string {
    keys := make([]string, 0)
    for k := range params { keys = append(keys, k) }
    sort.Strings(keys)
    var parts []string
    for _, k := range keys { parts = append(parts, fmt.Sprintf("%s=%s", k, params[k])) }
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write([]byte(strings.Join(parts, "&")))
    return hex.EncodeToString(mac.Sum(nil))
}

func SignOKX(timestamp, method, path, body, secret string) string {
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write([]byte(timestamp + method + path + body))
    return base64.StdEncoding.EncodeToString(mac.Sum(nil))
}
```

---

## �🌍 Thích Ứng Khu Vực

`binance&bybit&okx P2P auto_payment_bot` về lý thuyết hỗ trợ **bất kỳ ứng dụng thanh toán Android nào trên toàn cầu**.

| Khu Vực | Ứng Dụng Thanh Toán | Tích Hợp |
|---------|---------------------|----------|
| 🇵🇭 Philippines | Maya, GCash, UnionBank | Tự động hóa thông minh |
| 🇨🇳 Trung Quốc | Alipay, WeChat Pay | Tự động hóa thông minh |
| 🇮🇳 Ấn Độ | PhonePe, Google Pay, Paytm | Tự động hóa thông minh |
| 🇮🇩 Indonesia | Dana, OVO, GoPay | Tự động hóa thông minh |
| 🇻🇳 Việt Nam | MoMo, ZaloPay, Vietcombank | Tự động hóa thông minh |
| 🇹🇭 Thái Lan | PromptPay, TrueMoney | Tự động hóa thông minh |
| 🇹🇷 Thổ Nhĩ Kỳ | Papara, İş Bankası | Tự động hóa thông minh |
| �🇷 Hàn Quốc | Toss, KakaoPay | Tự động hóa thông minh |
| 🇭🇰 Hồng Kông | FPS, PayMe | Tự động hóa thông minh |
| 🇹🇼 Đài Loan | Ứng Dụng Ngân Hàng, LINE Pay | Tự động hóa thông minh |
| 🌐 Thị Trường Khác | Có thể thích ứng qua tự động hóa thiết bị thông minh | Mở rộng |

### Các Trường Hợp Sử Dụng Điển Hình

- 📈 **Thương Nhân Khối Lượng Lớn**: Xử lý hàng chục đến hàng trăm đơn P2P mỗi ngày
- 🌙 **Vận Hành Ban Đêm Không Giám Sát**: Tự động xử lý đơn ngoài giờ làm việc
- 📱 **Xử Lý Song Song**: Nhiều điện thoại chạy ứng dụng khác nhau đồng thời
- 🔗 **Vòng Lặp Tự Động Hóa Đầy Đủ**: Với `binance_p2p_bot` — từ liệt kê → đấu giá → đơn hàng → thanh toán → xác minh → giải phóng

---

## 🌐 Từ Khóa Hệ Sinh Thái

`binance&bybit&okx P2P auto_payment_bot`, `P2P auto payment`, `Binance P2P thanh toán tự động`, `Bybit P2P bot`, `OKX P2P bot`, `thanh toán P2P tự động`, `USDT giải phóng tự động`, `MoMo thanh toán tự động`, `ZaloPay auto payment`, `Binance C2C automation`, `P2P merchant automation`, `binance p2p bot`, `bybit p2p bot`.

---

## 💡 Câu Hỏi Thường Gặp

**H: `binance&bybit&okx P2P auto_payment_bot` là gì?**
Đ: Ứng dụng máy tính bàn tự động hóa cục bộ chuyên nghiệp cho thương nhân P2P Binance, Bybit và OKX. Khả năng cốt lõi là **thanh toán tự động theo đơn**. Cho phép giao dịch 24/7 không cần can thiệp.

**H: Có an toàn không? Cần máy chủ không?**
Đ: Hoạt động hoàn toàn cục bộ. API key chỉ được lưu trên máy tính của bạn. **Không bao giờ cấp quyền "Rút tiền" cho API key.**

**H: Mối quan hệ với `binance_p2p_bot`?**
Đ: Bổ sung cho nhau:
- [`binance_p2p_bot`](https://github.com/ApiP2P-top/binance-p2p-bot): Tự động định giá, xếp hạng (**quản lý trước đơn**)
- `binance&bybit&okx P2P auto_payment_bot`: Thanh toán, xác minh, giải phóng tự động (**thanh toán đơn hàng**)

**H: Sự khác biệt giữa "Thanh Toán Tự Động Theo Đơn" và "Giải Phóng Tự Động" truyền thống là gì?**
Đ: Giải pháp truyền thống chỉ xử lý bước giải phóng. `binance&bybit&okx P2P auto_payment_bot` đi xa hơn — **tự động thực hiện chuyển khoản**. Bộ ba hoàn chỉnh: **thanh toán + xác minh + giải phóng**.

**H: Phần mềm ngăn chặn giải phóng sai như thế nào (kiểm soát rủi ro)?**
Đ: Bảo vệ nhiều lớp: (1) Khớp số tiền chính xác; (2) Loại trùng ID toàn cầu (180 ngày); (3) Bảo vệ hết thời gian; (4) Giám sát số dư.

**H: Ứng dụng thanh toán / kênh nào được hỗ trợ?**
Đ: Về lý thuyết bất kỳ ứng dụng Android nào — nhờ bộ thích ứng thông minh. Chỉ cần cấu hình phương thức thanh toán. Chi tiết tại [apip2p.top](https://apip2p.top).

**H: Khởi động lại máy tính có mất cấu hình không?**
Đ: Không. Tất cả được lưu cục bộ real-time. Bot tự động khôi phục khi khởi động.

**H: `binance&bybit&okx P2P auto_payment_bot` sử dụng công nghệ gì?**
Đ: Xây dựng bằng Python cho trải nghiệm desktop native. Kiến trúc đa luồng. HMAC-SHA256 cho API. Cơ sở dữ liệu cục bộ. Không cần máy chủ ngoài.

**H: Bot có hỗ trợ tài sản biến động (BTC, ETH) không?**
Đ: Có. Logic thanh toán và xác minh hoạt động trên số tiền fiat. Với BTC/ETH, khuyến nghị sử dụng Spot Market Sync từ [`binance_p2p_bot`](https://github.com/ApiP2P-top/binance-p2p-bot).
---

## 🔗 Hệ Sinh Thái & Liên Hệ
* **Video Demo:** [🎬 Xem trên YouTube](https://www.youtube.com/watch?v=INewAPHXa5c)
* **Trang Chủ:** [apip2p.top](https://apip2p.top/)
* **Bot P2P Đồng Hành:** [ApiP2P-top/binance-p2p-bot](https://github.com/ApiP2P-top/binance-p2p-bot)
* **Telegram Nhà Phát Triển:** [@eason01993](https://t.me/eason01993)
* **Kênh Cộng Đồng:** [t.me/eason01993_p2p](https://t.me/eason01993_p2p)
* **YouTube:** [Kênh YouTube của Eason](https://www.youtube.com/@eason01993)

---

## ⚠️ Tuyên Bố Miễn Trừ

`binance&bybit&okx P2P auto_payment_bot` được cung cấp cho mục đích giáo dục và tham khảo. Giao dịch P2P crypto mang rủi ro đáng kể. Bảo quản API key an toàn. Không bao giờ cấp quyền "Rút tiền" cho công cụ tự động. Phần mềm không chịu trách nhiệm về tổn thất tài chính. Sử dụng hoàn toàn theo rủi ro của bạn.
