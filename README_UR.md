<div align="center">

**🌐 Select Language / 选择语言**

[English](README.md) | [中文](README_CN.md) | [Español](README_ES.md) | [Русский](README_RU.md) | [Bahasa Indonesia](README_ID.md) | [Türkçe](README_TR.md) | [Tiếng Việt](README_VN.md) | [ภาษาไทย](README_TH.md) | [हिन्दी](README_HI.md) | **اردو** | [Filipino](README_PH.md)

</div>

---

# binance-p2p-bot：binance&bybit&okx P2P auto_payment_bot: مکمل خودکار آرڈر ادائیگی · بل تصدیق · کرپٹو ریلیز بوٹ

> Binance, Bybit & OKX P2P تاجروں کے لیے پیشہ ورانہ آٹومیشن ٹول — **آرڈر کے مطابق خودکار ادائیگی**، **ذہین بل تصدیق**، **ملی سیکنڈ کرپٹو ریلیز**، ملٹی ڈیوائس متوازی پروسیسنگ

# [P2P خودکار ادائیگی اور ریلیز بوٹ (Binance & Bybit & OKX) سرکاری ویب سائٹ](https://apip2p.top)

## 🎬 ویڈیو ڈیمو


https://github.com/user-attachments/assets/262a41b2-cdcc-4abd-8c8e-4bdb1b566a03


[![ڈیمو دیکھیں](https://img.shields.io/badge/YouTube-ڈیمو_دیکھیں-red?style=for-the-badge&logo=youtube&logoColor=white)](https://www.youtube.com/watch?v=INewAPHXa5c)

> 📺 `binance&bybit&okx P2P auto_payment_bot` کا مکمل ویڈیو ڈیمو دیکھیں — آرڈر کی شناخت سے خودکار ادائیگی، بل تصدیق اور کرپٹو ریلیز تک

P2P تاجروں (اشتہاری) کے لیے خاص طور پر ڈیزائن کیا گیا خودکار آرڈر ادائیگی حل۔ دستی ٹرانسفر، دستی تصدیق اور دستی کرپٹو ریلیز کے چکر کو ختم کریں۔ `binance&bybit&okx P2P auto_payment_bot` کے ساتھ **مکمل 24/7 آٹومیشن** حاصل کریں — آرڈر کے مطابق ادائیگی، جمع تصدیق اور فوری کرپٹو ریلیز۔

---

## 📝 جائزہ اور فن تعمیر

<img width="1360" height="881" alt="2" src="https://github.com/user-attachments/assets/b2c18af8-6b0f-4062-a947-bb10e99aeed7" />


`binance&bybit&okx P2P auto_payment_bot` ایک **پیشہ ورانہ مقامی ڈیسک ٹاپ آٹومیشن ایپلیکیشن** ہے جو **Binance**، **Bybit** اور **OKX** پر P2P تاجروں (اشتہاری) کے لیے بنائی گئی ہے۔

P2P ٹریڈنگ میں تاجروں کو بار بار آپریشنز کا سامنا ہوتا ہے:

- ❌ **دستی ادائیگی**: ادائیگی ایپ کھولیں، رقم درج کریں، ٹرانسفر کی تصدیق کریں
- ❌ **دستی تصدیق**: ایپ کھولیں اور چیک کریں کہ خریدار نے ادائیگی کی ہے یا نہیں
- ❌ **دستی ریلیز**: ایکسچینج پر واپس جائیں اور ریلیز بٹن دبائیں
- ❌ **رات کا خلا**: غیر کاری اوقات میں آرڈرز جمع ہوتے ہیں

`binance&bybit&okx P2P auto_payment_bot` **ذہین ڈیوائس آٹومیشن + ایکسچینج API انٹیگریشن** کے ذریعے سب حل کرتا ہے:

- ✅ **🔥 آرڈر کے مطابق خودکار ادائیگی (بنیادی خصوصیت)**: ایکسچینج خرید آرڈر وصول کرتا ہے → ادائیگی ایپ میں خودکار ٹرانسفر → مداخلت کی ضرورت نہیں
- ✅ **ذہین بل تصدیق**: خریدار ادائیگی نشان لگاتا ہے → ایپ کھول کر رقم اور بھیجنے والے کی معلومات خودکار تصدیق
- ✅ **ملی سیکنڈ کرپٹو ریلیز**: کامیاب تصدیق کے بعد فوری ایکسچینج API کال
- ✅ **ملٹی ڈیوائس متوازی پروسیسنگ**: بیک وقت متعدد فونز جوڑیں
- ✅ **ملٹی ایکسچینج یکجا انتظام**: Binance, Bybit, OKX — ایک انٹرفیس
- ✅ **مکمل مقامی آپریشن**: کوئی بیرونی سرور انحصار نہیں

**پوزیشن**: `binance&bybit&okx P2P auto_payment_bot` **آرڈر ادائیگی + بل تصدیق + خودکار ریلیز** کو سنبھالتا ہے۔ **اینڈ ٹو اینڈ ذہین آٹومیشن** کے لیے [`binance_p2p_bot`](https://github.com/ApiP2P-top/binance-p2p-bot) کے ساتھ جوڑیں۔

---

## 🚀 بنیادی خصوصیات

### 1. 🔥 آرڈر کے مطابق خودکار ادائیگی

> **یہ `binance&bybit&okx P2P auto_payment_bot` کی سب سے اہم اور منفرد صلاحیت ہے۔**

- **ریئل ٹائم آرڈر شناخت**: 5 سیکنڈ polling سائیکل
- **ذہین ڈیوائس تقسیم**: ادائیگی طریقے کے مطابق خودکار مماثلت
- **خودکار ادائیگی عمل**: مکمل ٹرانسفر عمل
- **موافق ادائیگی انجن**: ذہین پروٹوکول انجن ہر ادائیگی ایپ کے انٹرفیس کے ساتھ خود بخود موافق ہوتا ہے
- **ملٹی ادائیگی طریقے**: ایک ڈیوائس، متعدد ادائیگی طریقے
- **اسکرین شاٹ محفوظ**: ہر لین دین کے بعد خودکار ثبوت کیپچر

#### 💡 نفاذ کا تصور (ٹرانسفر شیڈولنگ انجن)

```python
# [Pseudocode] Order detection → device matching → auto transfer scheduling pipeline
import asyncio
from dataclasses import dataclass
from typing import Optional, List

@dataclass
class TransferTask:
    order_id:       str
    amount:         float
    currency:       str
    payee_name:     str
    payee_account:  str
    payment_method: str
    status:         str

class TransferScheduler:
    POLL_INTERVAL = 5

    def __init__(self, exchange_clients: dict, device_pool: 'DevicePool'):
        self.exchanges   = exchange_clients
        self.device_pool = device_pool

    async def run(self):
        while True:
            for platform, client in self.exchanges.items():
                orders = await client.get_pending_orders()
                for order in orders:
                    if self._is_new_order(order):
                        await self._dispatch(order)
            await asyncio.sleep(self.POLL_INTERVAL)

    async def _dispatch(self, order) -> None:
        device = self.device_pool.find_idle_device(order.payment_method)
        if not device:
            return
        task = TransferTask(
            order_id=order.id, amount=order.fiat_amount,
            payee_name=order.counterparty, payee_account=order.account,
            payment_method=order.payment_method, status="pending",
        )
        await device.execute_transfer(task)
```

```javascript
// [Pseudocode] Node.js style — Order polling and device dispatch
class TransferScheduler {
    constructor(exchangeClients, devicePool) {
        this.exchanges  = exchangeClients;
        this.devicePool = devicePool;
        this.pollInterval = 5000;
    }

    async start() {
        while (true) {
            for (const [platform, client] of Object.entries(this.exchanges)) {
                const orders = await client.getPendingOrders();
                for (const order of orders) {
                    if (this.isNewOrder(order)) {
                        await this.dispatch(order);
                    }
                }
            }
            await this.sleep(this.pollInterval);
        }
    }

    async dispatch(order) {
        const device = this.devicePool.findIdleDevice(order.paymentMethod);
        if (!device) return;
        await device.executeTransfer({
            orderId: order.id,
            amount: order.fiatAmount,
            payeeName: order.counterparty,
            paymentMethod: order.paymentMethod,
        });
    }
}
```

---

### 2. ذہین بل تصدیق

- **خودکار بل نیویگیشن**: ایپ کو لین دین تاریخ صفحے پر کھولیں
- **درست رقم مماثلت**: قابل ترتیب برداشت کے ساتھ
- **بھیجنے والے کی تصدیق**: کراس ریفرنس بھیجنے والے کا نام
- **لین دین ID نکالنا**: Reference Number / لین دین کوڈ پڑھیں
- **اینٹی ری پلے تحفظ**: ہر ID ریکارڈ — ایک ادائیگی دو بار ریلیز کو متحرک نہیں کر سکتی

#### 💡 نفاذ کا تصور (ڈپازٹ تصدیق انجن)

```go
// [Pseudocode] Go style — Deposit verification with replay-attack protection
package verifier

type BillVerifyResult struct {
    Matched     bool
    Amount      float64
    SenderName  string
    UniqueID    string
}

type BillVerifier struct {
    archive    *BillArchive
    tolerance  float64
}

func (v *BillVerifier) Verify(order P2POrder) (*BillVerifyResult, error) {
    bills, err := v.navigateToBillList(order.PaymentApp)
    if err != nil {
        return nil, err
    }
    for _, bill := range bills {
        if math.Abs(bill.Amount - order.FiatAmount) <= v.tolerance {
            detail := v.openBillDetail(bill)
            if v.archive.Exists(detail.UniqueID) {
                continue
            }
            return &BillVerifyResult{
                Matched: true, Amount: detail.Amount,
                SenderName: detail.SenderName, UniqueID: detail.UniqueID,
            }, nil
        }
    }
    return &BillVerifyResult{Matched: false}, nil
}
```

```typescript
// [Pseudocode] TypeScript style — Bill verification service
interface BillDetail {
    amount: number; senderName: string; uniqueId: string; timestamp: Date;
}

class BillVerifyService {
    private archive: BillArchive;
    private tolerance = 0.02;

    async verify(order: P2POrder): Promise<VerifyResult> {
        const bills = await this.navigateToBillList(order.paymentApp);
        const matched = bills.find(bill =>
            Math.abs(bill.amount - order.fiatAmount) <= this.tolerance
            && !this.archive.exists(bill.uniqueId)
        );
        if (!matched) return { success: false };
        this.archive.record(matched.uniqueId, order.orderId);
        return { success: true, amount: matched.amount,
            senderName: matched.senderName, uniqueId: matched.uniqueId };
    }
}
```

---

### 3. خودکار کرپٹو ریلیز

- **فوری ریلیز**: تصدیق کے بعد فوری ایکسچینج API کال
- **نتیجے کی تصدیق**: آرڈر اسٹیٹس چیک
- **ایکسپونینشل ری ٹرائی**: زیادہ سے زیادہ 3 بار (2/4/8 سیکنڈ)
- **تین پلیٹ فارم سوئچر**: Binance / Bybit / OKX

#### 💡 نفاذ کا تصور (کراس پلیٹ فارم ریلیز ایگزیکیوٹر)

```python
# [Pseudocode] Cross-platform release executor
import asyncio, logging
logger = logging.getLogger("release_executor")

class PlatformAdapterFactory:
    @staticmethod
    def create(platform: str, credentials: dict):
        if platform == "binance": return BinanceP2PAdapter(credentials)
        elif platform == "bybit": return BybitP2PAdapter(credentials)
        elif platform == "okx": return OkxP2PAdapter(credentials)
        raise ValueError(f"Unsupported platform: {platform}")

class ReleaseExecutor:
    MAX_RETRIES = 3
    RETRY_BASE_S = 2

    async def release(self, platform: str, order_id: str, verify_result: dict) -> bool:
        adapter = PlatformAdapterFactory.create(platform, self.credentials)
        for attempt in range(self.MAX_RETRIES):
            try:
                resp = await adapter.release_order(order_id)
                if resp.success:
                    confirmed = await self._poll_completion(adapter, order_id)
                    if confirmed:
                        logger.info(f"[RELEASED] {platform} order {order_id}")
                        return True
            except Exception as e:
                wait = self.RETRY_BASE_S ** (attempt + 1)
                await asyncio.sleep(wait)
        return False
```

```rust
// [Pseudocode] Rust style — Release execution with retry
use std::time::Duration;

struct ReleaseExecutor { max_retries: u32, retry_base: Duration }

impl ReleaseExecutor {
    async fn release(&self, platform: &str, order_id: &str) -> Result<bool, Error> {
        let adapter = create_platform_adapter(platform)?;
        for attempt in 0..self.max_retries {
            match adapter.release_order(order_id).await {
                Ok(resp) if resp.success => {
                    if self.poll_completion(&adapter, order_id).await? {
                        return Ok(true);
                    }
                }
                Err(e) => {
                    let wait = self.retry_base * 2u32.pow(attempt);
                    tokio::time::sleep(wait).await;
                }
                _ => {}
            }
        }
        Ok(false)
    }
}
```

---

### 4. ملٹی ڈیوائس متوازی پروسیسنگ

- **ملٹی ڈیوائس کنکشن**: متعدد Android فونز کے لیے USB / WiFi
- **اسکرین میررنگ**: 30-60fps / 30-70ms تاخیر
- **ذہین تقسیم**: ایپ کے مطابق خودکار ڈیوائس مماثلت
- **آزاد قطار**: ہر ڈیوائس کی اپنی FIFO قطار
- **اسٹیٹس نگرانی**: کنکشن، موجودہ کام، بیلنس
- **ورک فلو الگ تھلگ**: مختلف ڈیوائسز × ایپس × ادائیگی طریقوں کے لیے الگ آپریشن فلو

#### 💡 نفاذ کا تصور (متوازی شیڈولر)

```python
# [Pseudocode] Multi-device task scheduler
from dataclasses import dataclass, field
from typing import Dict, List
from collections import deque

@dataclass
class Device:
    serial: str
    alias: str
    status: str
    supported_apps: List[str]
    task_queue: deque = field(default_factory=deque)

class DeviceScheduler:
    def __init__(self):
        self.devices: Dict[str, Device] = {}

    def find_idle_device(self, payment_method: str) -> Device | None:
        for device in self.devices.values():
            if device.status == "idle" and payment_method in device.supported_apps:
                return device
        return None

    def enqueue_task(self, task: 'TransferTask') -> bool:
        device = self.find_idle_device(task.payment_method)
        if device:
            device.task_queue.append(task)
            return True
        return False
```

```javascript
// [Pseudocode] JavaScript style — Device manager
class DeviceScheduler {
    constructor() { this.devices = new Map(); }

    findIdleDevice(paymentMethod) {
        for (const device of this.devices.values()) {
            if (device.status === 'idle' && device.supportedApps.includes(paymentMethod))
                return device;
        }
        return null;
    }

    enqueueTask(task) {
        const device = this.findIdleDevice(task.paymentMethod);
        if (device) { device.taskQueue.push(task); device.status = 'busy'; return true; }
        return false;
    }
}
```

---

### 5. ذہین ادائیگی موافقت انجن

- **بغیر ترتیب ادائیگی سپورٹ**: انجن خود بخود کسی بھی ادائیگی ایپ کے انٹرفیس کو پہچانتا اور موافق ہوتا ہے
- **کوڈنگ کی ضرورت نہیں**: بس اپنا ڈیوائس جوڑیں اور ادائیگی کا طریقہ ترتیب دیں
- **ملٹی ڈائمینشنل آپٹیمائزیشن**: ڈیوائس × ایپ × ادائیگی طریقہ کے مطابق آپٹیمائز شدہ عملداری راستے
- **خودکار اسناد انجیکشن**: پاس ورڈ اور PIN رن ٹائم پر مقامی خفیہ ترتیب سے انجیکٹ ہوتے ہیں
- **ڈوئل موڈ ایگزیکیوشن**: ادائیگی عمل (باہر جانے والی ٹرانسفرز) اور تصدیق (آنے والے ڈپازٹس کی جانچ) آزاد پائپ لائنز استعمال کرتے ہیں

---

### 6. رسک کنٹرول اور دھوکہ دہی کی روک تھام

- **ڈپلیکیٹ لین دین ID فلٹر**: 180 دن تک محفوظ
- **درست رقم مماثلت**: برداشت سے زیادہ ہونے پر رد
- **ٹائم آؤٹ تحفظ**: جمع نہ ملنے پر ریلیز نہیں
- **بیلنس نگرانی**: ناکافی بیلنس پر خودکار توقف
- **مکمل مقامی آرکائیو**: تمام لین دین کے ریکارڈ مقامی طور پر محفوظ (180 دن کی برقراری، خودکار صفائی)

---

### 7. اسٹیٹ سیو اور سیشن ریکوری

- **خودکار سیو**: تمام ترتیبات ریئل ٹائم محفوظ
- **کریش ریکوری**: آخری ترتیب خودکار لوڈ
- **زیر التوا آرڈر ریکوری**: اسٹارٹ اپ پر نامکمل آرڈرز اسکین

---

## �️ سسٹم آرکیٹیکچر

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Desktop GUI Main Window                          │
│  ┌───────────┐ ┌────────────┐ ┌────────────┐ ┌───────────┐  │
│  │ Exchange  │ │  Device     │ │  Screen    │ │ Log &     │  │
│  │ Management│ │ Management │ │  Mirroring │ │ Audit     │  │
│  └─────┬─────┘ └─────┬──────┘ └─────┬──────┘ └───────────┘  │
│        │             │             │                             │
│  ┌────▼───────────▼───────────▼──────────────────┐    │
│  │                Signal / Slot Event Bus                   │    │
│  └────┬───────────┬───────────┬──────────────────┘    │
│       │              │              │                              │
│  ┌────▼────┐ ┌─────▼─────┐ ┌────▼─────┐ ┌────────────┐   │
│  │  Order  │ │ Transfer  │ │   Bill   │ │   State    │   │
│  │ Poller  │ │ Executor  │ │ Verifier │ │  Persist   │   │
│  │(5s cycle)│ │(payment)  │ │(deposit) │ │ (JSON/DB)  │   │
│  └──────────┘ └───────────┘ └──────────┘ └────────────┘   │
│       │              │              │                              │
│  ┌────▼───────────▼───────────▼──────────────────┐    │
│  │        Anti-Replay & Risk Control Layer              │    │
│  │  ┌───────────┐ ┌───────────┐ ┌────────────────┐   │    │
│  │  │Amount     │ │Transaction│ │Balance Monitor   │   │    │
│  │  │Tolerance  │ │ID Dedup   │ │& Timeout Guard   │   │    │
│  │  └───────────┘ └───────────┘ └────────────────┘   │    │
│  └──────────────────────────────────────────────────┘    │
└───────────────────────────┬────────────────────────────────────┘
                            │
              ┌───────────▼──────────────┐
              │  Exchange API Adapter Layer  │
              │ ┌───────┐┌──────┐┌──────┐ │
              │ │Binance││Bybit ││ OKX  │ │
              │ └───────┘└──────┘└──────┘ │
              └──────────────────────────┘
```

---

## ⚙️ تکنیکی تفصیلات

| جزو | پیرامیٹر | تفصیل |
|------|-----------|--------|
| Polling سائیکل | 5,000 ms | ہر ایکسچینج کے لیے آزاد پولنگ |
| رقم برداشت | 0.01–0.10 | قابل ترتیب |
| Retry وقفہ | Exp. backoff 2/4/8s | زیادہ سے زیادہ 3 بار |
| Dedup برقراری | 180 دن | خودکار صفائی |
| میررنگ FPS | 30–60 fps | کم تاخیر (30-70ms) |
| کنکرنسی | لامحدود | USB + WiFi |
| ورک فلو پروفائلز | مقامی انکرپٹڈ اسٹور | ڈیوائس × ایپ × طریقہ کے مطابق |
| UI تھیمز | ڈارک / لائٹ | دو موڈز |
| UI زبانیں | 中文 / English | دو لسانی سپورٹ |

### API تصدیق اور درخواست دستخط

```python
# [Pseudocode] HMAC-SHA256 Request Signing
import hashlib, hmac, base64

class MultiPlatformAuth:
    def sign_binance(self, params: dict, secret: str) -> str:
        query = "&".join(f"{k}={v}" for k, v in sorted(params.items()))
        return hmac.new(secret.encode(), query.encode(), hashlib.sha256).hexdigest()

    def sign_bybit(self, timestamp: str, api_key: str, payload: str, secret: str) -> str:
        sign_str = f"{timestamp}{api_key}5000{payload}"
        return hmac.new(secret.encode(), sign_str.encode(), hashlib.sha256).hexdigest()

    def sign_okx(self, timestamp: str, method: str, path: str, body: str, secret: str) -> str:
        sign_str = f"{timestamp}{method}{path}{body}"
        return base64.b64encode(hmac.new(secret.encode(), sign_str.encode(), hashlib.sha256).digest()).decode()
```

```go
// [Pseudocode] Go style — Multi-platform API signing
package auth
import ("crypto/hmac"; "crypto/sha256"; "encoding/base64"; "encoding/hex"; "fmt"; "sort"; "strings")

func SignBinance(params map[string]string, secret string) string {
    keys := make([]string, 0)
    for k := range params { keys = append(keys, k) }
    sort.Strings(keys)
    var parts []string
    for _, k := range keys { parts = append(parts, fmt.Sprintf("%s=%s", k, params[k])) }
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write([]byte(strings.Join(parts, "&")))
    return hex.EncodeToString(mac.Sum(nil))
}

func SignOKX(timestamp, method, path, body, secret string) string {
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write([]byte(timestamp + method + path + body))
    return base64.StdEncoding.EncodeToString(mac.Sum(nil))
}
```

---

## �🌍 علاقائی موافقت

`binance&bybit&okx P2P auto_payment_bot` نظریاتی طور پر **دنیا بھر کی کسی بھی Android ادائیگی ایپ** کی حمایت کرتا ہے۔

| علاقہ | ادائیگی ایپس | انٹیگریشن |
|-------|-------------|-----------|
| 🇵🇰 پاکستان | JazzCash, Easypaisa, HBL | ذہین آٹومیشن |
| 🇵🇭 فلپائن | Maya, GCash, UnionBank | ذہین آٹومیشن |
| 🇨🇳 چین | Alipay, WeChat Pay | ذہین آٹومیشن |
| 🇮🇳 بھارت | PhonePe, Google Pay, Paytm | ذہین آٹومیشن |
| 🇮🇩 انڈونیشیا | Dana, OVO, GoPay | ذہین آٹومیشن |
| 🇻🇳 ویتنام | MoMo, ZaloPay, Vietcombank | ذہین آٹومیشن |
| 🇹🇭 تھائی لینڈ | PromptPay, TrueMoney | ذہین آٹومیشن |
| 🇹🇷 ترکی | Papara, İş Bankası | ذہین آٹومیشن |
| �🇷 جنوبی کوریا | Toss, KakaoPay | ذہین آٹومیشن |
| 🇭🇰 ہانگ کانگ | FPS, PayMe | ذہین آٹومیشن |
| 🇹🇼 تائیوان | بینک ایپس، LINE Pay | ذہین آٹومیشن |
| 🌐 دیگر مارکیٹس | ذہین ڈیوائس آٹومیشن سے موافقت | قابل توسیع |

### عام استعمال کے معاملات

- 📈 **زیادہ حجم والے تاجر**: روزانہ درجنوں سے سیکڑوں P2P آرڈرز کی پروسیسنگ
- 🌙 **رات کی بغیر نگرانی آپریشن**: غیر کاری اوقات میں خودکار آرڈر پروسیسنگ
- 📱 **متوازی پروسیسنگ**: متعدد فونز بیک وقت مختلف ایپس چلاتے ہیں
- 🔗 **مکمل آٹومیشن لوپ**: `binance_p2p_bot` کے ساتھ — لسٹنگ سے کرپٹو ریلیز تک

---

## 🌐 ایکو سسٹم کیورڈز

`binance&bybit&okx P2P auto_payment_bot`, `P2P auto payment`, `Binance P2P خودکار ادائیگی`, `Bybit P2P bot`, `OKX P2P bot`, `P2P خودکار ادائیگی`, `USDT خودکار ریلیز`, `JazzCash خودکار ادائیگی`, `Easypaisa auto payment`, `Binance C2C automation`, `P2P merchant automation`, `binance p2p bot`, `bybit p2p bot`

---

## 💡 اکثر پوچھے جانے والے سوالات

**س: `binance&bybit&okx P2P auto_payment_bot` کیا ہے؟**
ج: Binance, Bybit اور OKX P2P تاجروں کے لیے پیشہ ورانہ مقامی ڈیسک ٹاپ آٹومیشن ایپلیکیشن۔ بنیادی صلاحیت **آرڈر کے مطابق خودکار ادائیگی** ہے۔ بغیر مداخلت 24/7 ٹریڈنگ فعال کرتا ہے۔

**س: کیا یہ محفوظ ہے؟ کیا سرور کی ضرورت ہے؟**
ج: مکمل مقامی آپریشن۔ API key صرف آپ کے کمپیوٹر پر محفوظ ہے۔ **API key کو کبھی "واپسی" اجازت نہ دیں۔**

**س: `binance_p2p_bot` سے تعلق؟**
ج: ایک دوسرے کے تکمیلی:
- [`binance_p2p_bot`](https://github.com/ApiP2P-top/binance-p2p-bot): خودکار قیمت مقرر، درجہ بندی (**آرڈر سے پہلے انتظام**)
- `binance&bybit&okx P2P auto_payment_bot`: ادائیگی، تصدیق، خودکار ریلیز (**آرڈر ادائیگی**)

**س: "آرڈر کے مطابق خودکار ادائیگی" اور روایتی "آٹو ریلیز" میں کیا فرق ہے؟**
ج: روایتی حل صرف ریلیز مرحلے کو سنبھالتے ہیں۔ `binance&bybit&okx P2P auto_payment_bot` آگے بڑھتا ہے — **خودکار ٹرانسفر** ادائیگی ایپ میں انجام دیتا ہے۔ یہ مکمل تین گنا ہے: **ادائیگی + تصدیق + ریلیز**۔

**س: سافٹ ویئر غلط ریلیز کو کیسے روکتا ہے (رسک کنٹرول)؟**
ج: ملٹی لیئر تحفظ: (1) درست رقم مماثلت؛ (2) لین دین ID عالمی ڈیڈپلیکیشن (180 دن)؛ (3) ٹائم آؤٹ تحفظ؛ (4) بیلنس نگرانی۔

**س: کون سی ادائیگی ایپس سپورٹ ہیں؟**
ج: نظریاتی طور پر تمام Android ایپس — ذہین موافقت انجن کے ذریعے۔ بس ادائیگی طریقہ ترتیب دیں۔ تفصیلات [apip2p.top](https://apip2p.top) پر۔

**س: ری اسٹارٹ پر ترتیب ضائع ہوتی ہے؟**
ج: نہیں۔ سب کچھ ریئل ٹائم مقامی طور پر محفوظ۔ اسٹارٹ اپ پر خودکار بحالی۔

**س: کون سا ٹیکنالوجی اسٹیک استعمال ہوتا ہے؟**
ج: Python ڈیسک ٹاپ تجربے کے لیے۔ ملٹی تھریڈ آرکیٹیکچر۔ HMAC-SHA256 API کے لیے۔ مقامی ڈیٹا بیس۔

**س: متغیر اثاثے (BTC, ETH) سپورٹ ہیں؟**
ج: ہاں۔ ادائیگی کی منطق فیاٹ رقم پر کام کرتی ہے۔ BTC/ETH کے لیے [`binance_p2p_bot`](https://github.com/ApiP2P-top/binance-p2p-bot) کی Spot Market Sync تجویز ہے۔

---

## 🔗 ایکو سسٹم اور رابطہ

* **ویڈیو ڈیمو:** [🎬 YouTube پر دیکھیں](https://www.youtube.com/watch?v=INewAPHXa5c)
* **سرکاری ویب سائٹ:** [apip2p.top](https://apip2p.top/)
* **ساتھی P2P بوٹ:** [ApiP2P-top/binance-p2p-bot](https://github.com/ApiP2P-top/binance-p2p-bot)
* **ڈیولپر Telegram:** [@eason01993](https://t.me/eason01993)
* **کمیونٹی چینل:** [t.me/eason01993_p2p](https://t.me/eason01993_p2p)
* **YouTube:** [Eason کا YouTube چینل](https://www.youtube.com/@eason01993)

---

## ⚠️ دستبرداری

`binance&bybit&okx P2P auto_payment_bot` تعلیمی اور حوالہ مقاصد کے لیے فراہم کیا گیا ہے۔ P2P کرپٹو ٹریڈنگ میں اہم خطرات ہیں۔ API key محفوظ رکھیں۔ آٹومیشن ٹولز کو کبھی "واپسی" اجازت نہ دیں۔ سافٹ ویئر مالی نقصان کا ذمہ دار نہیں ہے۔ اپنے خطرے پر استعمال کریں۔
